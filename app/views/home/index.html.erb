<div class="header">
  <div class="filter">
    <div class="section">
      <h1>The coolest cat from the net</h1>
      <p>Get your pictures right now!</p>
    </div>
  </div>
</div>

<div class="content">
    <% @cats.each do |cat| %>
    <div class="main">
      <a href="items/<%= cat.id %>">
     <%= image_tag(cat.image_url) %>
      <h3><%= cat.title %></h3>
      <br><hr>
      <h1><%= cat.price %>€</h1>
      <p><%= cat.description %></p>
      </a>
    </div>
      <% end %>
</div>
